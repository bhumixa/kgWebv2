import { SortCodesPipe } from './sort-codes.pipe';

describe('SortCodesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCodesPipe();
    expect(pipe).toBeTruthy();
  });
});
